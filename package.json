{
  "dependencies": {
    "celsius": "^1.0.2",
    "convert-length": "^1.0.1",
    "express": "^4.19.2"
  },
  "scripts": {
    "docker:build": "docker build -t schreglmann1/wetter_to_prom:latest .",
    "docker:publish": "docker push schreglmann1/wetter_to_prom:latest"
  }
}
